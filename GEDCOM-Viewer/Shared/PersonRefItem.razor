@if (Person != null)
{
    <NavLink href="@string.Format("view-person/{0}", Person.Id)">
        @(string.Format("{0} {1}", Person.FirstName, Person.LastName).Trim())
    </NavLink>
}



@code {
    [Parameter]
    public Person? Person { get; set; }
}

